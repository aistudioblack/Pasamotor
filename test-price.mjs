import { createClient } from '@supabase/supabase-js'; async function run() { const sbUrl = process.env.VITE_SUPABASE_URL; const sbKey = process.env.VITE_SUPABASE_ANON_KEY; const supabase = createClient(sbUrl, sbKey); await supabase.auth.signInWithPassword({email: 'aistudioblack@gmail.com', password: 'PassWord123!'}); const supp = await supabase.from('suppliers').select('*').limit(1).single(); const userCode = supp.data.user_code; const password = supp.data.password_encrypted; const getReq = await fetch('https://siparis.fcs.com.tr/Login'); const c1 = getReq.headers.get('set-cookie')?.split(';')[0]; const loginRes = await fetch('https://siparis.fcs.com.tr/Login/Index', { method: 'POST', headers: { 'Content-Type': 'Application/json;charset=utf-8', 'Cookie': c1, 'X-Requested-With': 'XMLHttpRequest' }, body: JSON.stringify({ CustomerCode: userCode, UserCode: userCode, Password: password, LanguageId: 1, Captcha: '', NewPassword: '', NewPasswordRepeat: '', ChangePassword: false }) }); const lc = loginRes.headers.get('set-cookie')?.split(';')[0] || ''; const allCookies = [c1, lc].join('; '); const searchRes = await fetch('https://siparis.fcs.com.tr/Search/SearchProduct', { method: 'POST', headers: { 'Content-Type': 'application/json;charset=UTF-8', 'Cookie': allCookies, 'X-Requested-With': 'XMLHttpRequest' }, body: JSON.stringify({ dataCount: 0, t9Text: 'MSK246', manufacturer: '', orderby: '4', productGroup1: 'MOTOSİKLET', productGroup2: '', productGroup3: null, vehicleBrand: '', vehicleModel: null, campaign: false, newArrival: false, newProduct: false, comparsionProduct: false, onQuantity: false, onWay: false, directSearch: false }) }); const data = await searchRes.json(); console.log(JSON.stringify(data.ProductList?.[0], null, 2)); } run();