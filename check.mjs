import { createClient } from '@supabase/supabase-js'; async function run() { const sbUrl = process.env.VITE_SUPABASE_URL || ''; const sbKey = process.env.VITE_SUPABASE_ANON_KEY || ''; if (!sbUrl) return console.log('No SB URL'); const supabase = createClient(sbUrl, sbKey); const { data, error } = await supabase.from('products').select('id, slug, sku, title'); console.log('Total DB Products:', data?.length || 0);  const bySku = new Map(); const duplicates = []; (data||[]).forEach(p => { if (bySku.has(p.sku)) { duplicates.push(p); } else { bySku.set(p.sku, p); } }); console.log('Unique SKUs:', bySku.size); console.log('Duplicates Count:', duplicates.length); if (duplicates.length > 0) { console.log('Deleting duplicates...'); const dupIds = duplicates.map(d => d.id); for (let i=0; i<dupIds.length; i+=50) { const chunk = dupIds.slice(i, i+50); await supabase.from('products').delete().in('id', chunk); } console.log('Deleted'); } } run();